extends default

block content

    link(rel="stylesheet", href="/stylesheets/containerBox.css")

    div(class="container-fluid container-box")
        h2 #{title}
            each product in products
                .row
                    a(href=`/products/${product.id}`, class="text-secondary") #{product.name}
